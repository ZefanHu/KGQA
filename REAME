数据库层
基于结构化IMDB的电影数据
根据数据以及属性图模式，创建E_R图
根据E_R图，以及load csv语法把节点和关系导入neo4j

分析自然问题并总结出一些类别
使用分类算法把自然问句进行分词，对应到预先定义的类别中
把预先定义的类别根据相应的查询语句，从neo4j中查询。

构造训练数据，根据电影场景手动或者使用聚类算法得出主要的类别，如评分、上映、风格... ...
每个分类在通过正则或者聚类得出可能的问法。nr代表某个人，nm代表电影。
文件名就是label，文件内容就是训练数据（每一行就是一个训练数据）